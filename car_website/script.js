const brand = document.getElementById ("brand");
const model = document.getElementById("model");
const year = document.getElementById("year");
const registerButton = document.getElementById("registerButton")
registerButton.addEventListener("click", function (event) {event.preventDefault()
console.log("Car registration started");
const currentBrand = brand.value
console.log(currentBrand)
const currentModel = model.value
console.log(currentModel)
const currentYear = year.value
console.log(currentYear)});

registerButton.addEventListener(const carForm = document.getElementById("carForm")

